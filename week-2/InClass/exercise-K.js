const estudents = [
    "Toño",
    "Ronar",
    "Diego",
    ];

    for (let i = 0; i < estudents.length; i++) {
        var mensaje = estudents[i] + " es alumno de migracode"
        console.log(mensaje)
        }